APP_PORT=8080
GIN_MODE=release

POSTGRES_DB=rentvibe
POSTGRES_USER=rentvibe
POSTGRES_PASSWORD=change-me
# Used when running the API directly outside Docker.
DATABASE_URL=postgres://rentvibe:change-me@localhost:5432/rentvibe?sslmode=disable

REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRATION_HOURS=24
